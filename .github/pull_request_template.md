## Summary

-

## Checks

- [ ] I ran the relevant local checks (`npm run format:check`, `npm run lint`, `npm run typecheck`, `npm run test:unit`, `npm run build`, and/or `npm run repo-hygiene`).
- [ ] I added or updated fixture/unit tests for parser, dispatcher, or model behavior where practical.
- [ ] I considered whether this changes an MCP tool name, input schema, or response shape.

## Scraper Evidence

If this PR adds or changes a live scraper, include:

- Store:
- Command run:
- ZIP/store ID used:
- Date/time run:
- Result count:
- Sample redacted output:
- Known limitations or flaky steps:

If this PR does not affect live scraping, write `N/A`.
